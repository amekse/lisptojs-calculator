/*
    Developing the command execution tree
*/

const checkSyntaxStructure = (commandList) => {}

export const initSA = (commandList) => {
    checkSyntaxStructure(commandList);
    return commandList;
}